# vp_models package: perception model implementations for vp_car_sim, split
# by inference backend into pth_pipeline (native PyTorch models/inference)
# and trt_pipeline (TensorRT-accelerated equivalents).
