# pth_pipeline package: groups the PyTorch (.pth) model definitions
# (model_components) and their inference wrappers (inference) used for
# camera-based scene segmentation and 3D/depth perception in vp_car_sim.
