# Model-components subpackage: the PyTorch building blocks (backbones,
# context/attention modules, decoder necks and heads) that are composed into
# the scene-segmentation and 3D/depth networks used by the pth_pipeline.
